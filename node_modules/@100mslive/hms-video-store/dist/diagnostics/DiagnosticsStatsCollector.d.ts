import { DiagnosticsRTCStatsReport } from '.';
import { HMSWebrtcStats } from '../rtc-stats';
import { HMSSdk } from '../sdk';
export declare class DiagnosticsStatsCollector {
    private sdk;
    private peerStatsList;
    private localAudioTrackStatsList;
    private localVideoTrackStatsList;
    private remoteAudioTrackStatsList;
    private remoteVideoTrackStatsList;
    constructor(sdk: HMSSdk);
    handleStatsUpdate(stats: HMSWebrtcStats): Promise<void>;
    buildReport(): DiagnosticsRTCStatsReport;
}
