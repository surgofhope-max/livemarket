import { HMSUpdateListener } from '../../interfaces';
import { Store } from '../../sdk/store';
import HMSTransport from '../../transport';
export declare class PollsManager {
    private store;
    private transport;
    listener?: HMSUpdateListener | undefined;
    constructor(store: Store, transport: HMSTransport, listener?: HMSUpdateListener | undefined);
    handleNotification(method: string, notification: any): void;
    private handlePollStart;
    private handlePollStop;
    private handlePollStats;
    private updatePollResult;
    private updatePollResponses;
}
