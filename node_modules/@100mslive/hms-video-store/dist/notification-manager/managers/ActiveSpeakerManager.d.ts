import { HMSAudioListener, HMSUpdateListener } from '../../interfaces';
import { Store } from '../../sdk/store';
import { SpeakerList } from '../HMSNotifications';
export declare class ActiveSpeakerManager {
    private store;
    listener?: HMSUpdateListener | undefined;
    audioListener?: HMSAudioListener | undefined;
    constructor(store: Store, listener?: HMSUpdateListener | undefined, audioListener?: HMSAudioListener | undefined);
    handleActiveSpeakers(speakerList: SpeakerList): void;
}
